/** Stable resource id until auth/projects exist. */
export const DEFAULT_CHAT_RESOURCE_ID = "anon";

/** Moderate recent-message window for chat-agent Memory. */
export const DEFAULT_LAST_MESSAGES = 20;

export const CHAT_MEMORY_STORAGE_ID = "chat-agent-storage";
